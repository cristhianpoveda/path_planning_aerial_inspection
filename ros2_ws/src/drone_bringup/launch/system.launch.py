"""Top-level launch — includes each service launch (skeleton placeholder).

Add IncludeLaunchDescription entries for camera_streamer / drone_localisation /
drone_navigation / drone_evaluation as the services come online.
"""
from launch import LaunchDescription


def generate_launch_description() -> LaunchDescription:
    return LaunchDescription([
        # TODO: include per-package launch files here.
    ])
